import { DcDirective } from './dc.directive';

describe('DcDirective', () => {
  it('should create an instance', () => {
    const directive = new DcDirective();
    expect(directive).toBeTruthy();
  });
});
